{%- comment -%} Filter Supply — generic page {%- endcomment -%}
<main class="wrap" style="padding:clamp(56px,8vw,120px) 0;max-width:760px;">
  <div class="sec-head" style="text-align:left;"><h1 class="h-serif">{{ page.title }}</h1></div>
  <div class="rte" style="font-family:var(--jp);font-weight:300;line-height:1.95;color:var(--ink-60);">{{ page.content }}</div>
</main>
