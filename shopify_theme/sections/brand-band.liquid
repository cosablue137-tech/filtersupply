{%- comment -%} Filter Supply — brand band (cream) {%- endcomment -%}
<section class="band fade">
  {%- if section.settings.eyebrow != blank -%}<p class="eyebrow">{{ section.settings.eyebrow }}</p>{%- endif -%}
  <h2 class="band__name">{{ section.settings.heading }}</h2>
  {%- if section.settings.body != blank -%}<p>{{ section.settings.body }}</p>{%- endif -%}
</section>

{% schema %}
{
  "name": "ブランドバンド",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "ラベル", "default": "Filter Supply · Since 2019" },
    { "type": "text", "id": "heading", "label": "見出し（セリフ大）", "default": "コーヒーを通して、素晴らしい時間を。" },
    { "type": "textarea", "id": "body", "label": "本文", "default": "2019年、福岡・高砂。自家焙煎のスペシャルティコーヒーだけに特化した、ハンドドリップ専門店です。一杯が生む時間を、ともに大切にしたいと考えています。" }
  ],
  "presets": [{ "name": "ブランドバンド" }]
}
{% endschema %}
